const textUsername = document.querySelectorAll('username');
const textPassword = document.querySelectorAll('password');
const btnLogin = document.querySelectorAll('btn-login');
const btnSignup = document.querySelectorAll('btn-signup');

